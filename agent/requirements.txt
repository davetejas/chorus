python-dotenv==1.0.1
numpy>=2.0

# LiveKit Agents + plugins as optional deps
livekit-agents[openai,silero]~=1.5

# Local STT
faster-whisper>=1.0.3

# Local TTS (installs piper binary into venv)
piper-tts==1.3.0
